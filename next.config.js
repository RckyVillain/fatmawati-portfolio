/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    formats: ['image/webp'],
  },
  // Optimize for static export if needed
  output: undefined,
};

module.exports = nextConfig;
